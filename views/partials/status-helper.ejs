<%
const statusColorMap = {
  pending: 'yellow',
  diproses: 'blue',
  dikirim: 'purple',
  selesai: 'green',
  dibatalkan: 'red',
};
function getStatusColor(status) {
  return statusColorMap[status] || 'gray';
}
%>
